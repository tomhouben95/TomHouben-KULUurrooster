@{
    ViewData["Title"] = "Home Page";
}
<div class="container-fluid login-container">
    <div class="col-md-4 col-md-offset-4">
        <div class="row">
            <a class="btn btn-primary btn-block" href="@Url.Action("FacebookLogin", "Account")" >Login with Facebook</a>
        </div>
        <div class="row">
            <hr class="hr-text" data-content="OR">
        </div>
        <div class="row">
            <a class="btn btn-danger btn-block" href="@Url.Action("GoogleLogin", "Account")">Login with Google</a>
        </div>
    </div>
</div>


